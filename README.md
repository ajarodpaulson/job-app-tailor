# job-app-tailor
A semi-deterministic tailor for resumes, cover letters, and job application questions.

`. .venv/bin/activate` // to activate the virtual environment
`python3 -m pip install -r requirements.txt` // to install deps in the virtual environment
